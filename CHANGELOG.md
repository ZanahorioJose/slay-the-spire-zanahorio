# CHANGELOG

本文件是项目的**开发日志**，按日期记录每次显著改动（新增/变更/修复/移除），供玩家与开发者快速了解项目演进。

- 格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，日期使用 ISO 8601（YYYY-MM-DD）
- 新条目在上、旧条目在下；每次完成改动后在文件顶部追加
- 同类改动分组：`### Added`（新增）/ `### Changed`（变更）/ `### Fixed`（修复）/ `### Removed`（移除），无内容的分组省略
- 未来发布版本时按 `## [x.y.z] - YYYY-MM-DD` 归档；未发布的新改动先记在 `## [Unreleased]`


## [Unreleased]

### Added
- 美术风格设计规范 v1（`docs/art-style.md`）：定位「像素角色 + 3D 材质界面（HD-2D 混搭）」，
  定义像素 Sprite 规格、卡牌材质分层（普通/罕见/稀有/金卡/闪卡）、UI 材质系统、
  背景三层分层、设计 Token 与 AI 出图 Prompt 模板
- 材质预览页 `docs/art-mockup.html`：浏览器直接打开即可查看，含五种稀有度卡牌、
  像素史莱姆待机动画、皮革面板/宝石条/金属按钮、三层背景主题示例
- 金卡（`.gold`）与闪卡（`.foil`）材质 class 预留：与稀有度字段解耦，未来掉落机制可独立设计
- 卡牌材质展示页 `docs/art-card-material.html`（原 `art-material-test.html` 更名）：纯 HTML/CSS/JS（无图片、无 WebGL），
  可实时切换六种材质（粗布/银/合金/金箔/箔面/玻璃）、拖参数调光照、开关 3D 跟随鼠标倾斜，
  并内置压力测试（30/60/120 张卡 + FPS 读数）。无头 Edge 实测：120 张全闪卡同时动画稳定 60 FPS；
  提供「高性能模式」开关（箔面不旋转只扫光）作为兜底
- 卡面素材支持配置切换：演示页脚本顶部素材配置区，`kind: "pixel"` 用内置占位像素画、
  `kind: "image"` 引用 `assets/` 实际图片；定下演示页命名约定
  `docs/art-<对象>-material.html`（未来怪物/背景演示页沿用）
- 卡牌材质展示页新增「选择本地图片」：文件选择器直接填充卡面
  （Object URL 加载，本地照片平滑显示不套像素锐化），可一键恢复占位像素画
- 卡牌材质展示页新增卡图缩放与 X/Y 轴微调：缩放 25%–400%、X/Y 偏移 -120px~120px，
  换图自动重置、可一键复位（CSS 变量驱动 transform，GPU 合成层不卡顿）
- 卡牌材质展示页材质库扩展至 18 种 CSS 材质技术：新增拉丝金属、液态铬、黄铜、碳纤维、
  珠光、油膜全息、大理石、木纹、石板、岩浆、星云、霓虹（重复渐变 / conic+混合模式 /
  动画背景层 / 扫光等技法），并新增「材质参考墙」一次对比全部材质、点卡片即切到主卡
- 新增 `docs/art-monster-material.html`（怪物材质演示）：像素精灵占位（两帧待机 canvas，
  可换本地图片 + 缩放/XY 微调）、待机/受击/攻击 CSS 动作占位、卡框复用 18 种材质 + 参考墙
- 新增 `docs/art-background-material.html`（背景材质演示）：三层纵深舞台
  （远景/中景/近景）、5 种场景主题（地牢/沼泽/熔岩/星云/大理石）、中景可换本地图片 + 缩放/XY
- 同步卡面归档进 `docs/`：材质调试页（`art-card-material.html`）+ 卡牌版式调试页
  （`art-card-layout_v4.html`，7 套方案）+ `emboss.md` 表面工艺方案 +
  `assets/sparkles.gif` 星星素材
- 卡牌/怪物材质演示页新增「宝可梦全息」材质（第 19 种）：sparkles.gif 星星层 + color-dodge +
  动态高光，技法参考 simeydotme/pokemon-cards-css
- `docs/art-style.md` 新增 §11 卡面结构定稿（v4）：8 层卡面结构、7 套方案、类型形状同心裁剪、
  全息闪卡与四类浮雕技术要点（含宝可梦 holo / iCSS 参考）
- 素材目录分工：新增 `assets/`（cards/enemies/relics/events/ui/backgrounds/packs），
  `docs/` 只放规范与讨论，实际素材与材质包统一进 `assets/`（见 `docs/art-style.md` §10）

### Changed
- 按方案一精简 docs/ 卡面归档：仅保留 `art-card-material.html`（材质调试）与
  `art-card-layout_v4.html`（卡牌调试），删除 v1/v2/v3 中间版归档副本
- `docs/emboss.md` 标题改为「卡牌表面工艺方案 · 浮雕与全息（Emboss & Holo Foil）」，
  定位为美术讨论内容沉淀；删除临时归档文件夹 `0814/`
- `docs/art-card-layout_v4.html` 全部卡面加入浮雕（bevel）：左上费用数字凸起、
  右上类型宝石凸起、下方描述面板凹陷嵌印，光源统一左上（文字保持平面）
- 修复稀有度宝石菱形切面：左上受光亮面 + 右下背光暗面 + 中间主色，
  替换原 45° 斜纹渐变（轮廓本就正确，问题在切面观感）
- 文档去重：动画分工矩阵（原 art-style §6）→ 唯一权威 `animation-guide.md` §6；
  Sprite 出图规范（原 animation-guide §5）→ 唯一权威 `art-style.md` §2；
  全息/浮雕细节（原 art-style §11.2）→ 唯一权威 `emboss.md`（新增 §6.4 产品决策摘要）
- 整合卡牌调试页为 `docs/art-card-studio.html`（卡牌美术工作台）：v4 卡面（7 套方案 /
  类型形状 / 稀有度 / 内容编辑）+ 19 种材质库（含参考墙，点卡片切换）+ 动画
  （全息膜 / 扫光 / 星星 / 3D 倾斜 / 浮雕）+ 本地卡图拖拽缩放，一站式设计卡牌美术；
  删除原 `art-card-material.html` 与 `art-card-layout_v4.html`
- 工作台扫光重做：锐利的黑白高对比带改为**高斯模糊的柔和扫光**（`blur(6px)` + 软渐变），
  并改为周期性自动扫过；新增「扫光频率（间隔）」「扫光速度（单次时长）」两个控制条，
  全息扫光与材质闪卡扫光带共用同一驱动
- 修正"白色线条"来源：那是星星层里的斜向条纹（`repeating-linear-gradient`），并非扫光；
  已移除条纹与 `streak-move` 动画，只保留真正的星星光点（`radial-gradient` + 闪烁），
  开关文案更新为「预览全息膜 / 扫光 / 星星」
- 修复 `docs/art-mockup.html` 与 `docs/art-card-material.html` 卡图溢出：
  16×16 像素画由放大 8 倍（128px）改为 6 倍（96px）并加 `overflow: hidden` 保护
- `docs/art-mockup.html` 卡牌 3D 倾斜改为跟随鼠标：双轴 `rotateX`/`rotateY`，
  镜面高光中心（`--lx`/`--ly`）同步移动，鼠标移出回正
- 修复闪卡箔面旋转角部超出卡面：旋转层由整块方框改为圆角裁剪容器内的伪元素
  （`overflow: hidden`），`conic-gradient` 绕卡面中心旋转，角部不再甩出

## 2026-08-16

### Added

- 怪物卡片测试页 `docs/art-monster-card.html`：探索「怪物 = 白边贴纸 + 卡牌材质」——
  默认**贴纸（异形卡）模式**：白边即卡缘，卡型随怪物轮廓（8 方向 `drop-shadow`
  均匀白边，PNG 图同样适用）；**不做厚度画法**（pokemon-cards-css 亦无实体厚度技法，
  只有 translateZ 光效分层），光泽一律不出白边 = 天然贴纸；
  可切换**矩形卡模式**（卡面材质 + mask 边框环 + 卡后侧边厚度，厚度滑杆仅该模式可用）。
  贴纸模式按「**图片层 + 材质层**」分层：怪物图印在<b>材质包边</b>上
  （贴纸边缘一圈材质色 `--mat-edge`，白边在材质外；材质包边宽可调，
  未来 PNG 用 alpha mask 做材质衬底，逻辑一致）。
  卡面材质复用卡牌体系（粗布/银/合金/金/闪卡/玻璃/珠光/油膜）、15 种怪物 emoji 切换、
  鼠标 3D 倾斜 + 自动旋转；白边粗细 / 厚度 / 倾斜幅度可调

### Fixed

- 怪物卡测试页打开卡死（真实浏览器 GPU 压力过大）：贴纸 emoji 上叠了
  **16 层 `drop-shadow`**（材质边 8 + 白边 8）且自动旋转每帧触发滤镜重绘——
  改为 **SVG `feMorphology` 双扩张描边**（一次滤镜完成材质边 + 白边，
  radius 随滑杆同步，渲染效率高一个量级）；自动旋转默认关闭（悬停才动）；
  参考条小图改用轻量 4 方向 drop-shadow。实测页面响应 1~5ms，视觉不变
- 进一步减负：**贴纸模式平面化**（不倾斜——贴纸无厚度，悬停不再每帧重绘
  描边滤镜，滤镜只静态渲染一次；倾斜开关贴纸模式禁用，矩形模式恢复）；
  **参考条小图滤镜归零**（此前 8 张小卡各 4 个 drop-shadow / 或继承 SVG 滤镜）。
  当前负担：贴纸主卡 1 个静态 SVG 滤镜 + 0 动画，mini 0 滤镜
- 贴纸恢复 3D 倾斜且不卡：描边改为**预烘焙**——`bakeSticker()` 用 canvas
  `ctx.filter` 把「emoji + 材质边 + 白边」一次性画成位图，运行时贴纸就是
  一张 canvas 纹理，倾斜 = GPU 挪纹理（与卡牌工作台同原理），零滤镜零重绘；
  白边/材质边/材质色/怪物变化时低频重烘焙；canvas 画不出内容时（如无头渲染
  限制）兜底回退 emoji 文本避免空白。实测贴纸倾斜恢复、响应 1ms
- 修复真机打不开/卡死：Canvas 2D filter 是软件渲染，16 层 drop-shadow 链
  一次性烘焙会忙住主线程——描边降为 **2 层模糊 drop-shadow**（材质边 + 白边，
  模糊半径 = 边宽，粗细一致），烘焙改**异步执行**（首屏先显示 emoji，
  requestAnimationFrame 后再烘焙）并加 **try/catch 兜底**，任何异常回退
  emoji 文本，绝不阻塞页面

## 2026-08-15

### Added

- 地图图例（主地图与 F 浮层均可用）：列出除战斗外的房间类型
  （宝箱 / 事件 / 篝火 / 精英 / 商店 / 先古 / 首领）图标说明；
  悬停图例项时对应类型节点金色高亮、其余节点临时变暗，移出恢复
- 新增 `docs/DEVELOPMENT.md` 开发者文档：承接原 README 中的功能清单、
  战斗事件触发顺序、核心文件与环境说明，README 聚焦对外展示
- `RunState.visitedNodes`：记录本幕已进入过的节点（去重写入、换幕清空），
  地图据此区分「已到达」与「未到达」；新增访问记录回归测试
- 背景演示页升级为**舞台剧式三景别**（`docs/art-background-material.html`）：
  远景=天幕布景（最糊、静止）、中景=表演区（焦点最清晰、轻飘动）、
  近景=台口剪影（微糊、飘动最快）；三景别独立高斯模糊滑杆 + 景深预设
  （舞台浅景深 / 微景深 / 全清晰）、中近景飘动幅度/速度可调可开关、
  保留本地图片中景替换与缩放；**默认主题「星火荒原」**：夜空星空/银河/月亮
  → 火山群（熔岩口光 + 月光侧光）→ 前草地（草叶风吹摆动）
- `docs/todo-monster-character-art.md`：怪物美术与任务角色美术待办
  （像素 Sprite Sheet、引擎接入、角色立绘字段）
- `MapNode.x`（行内水平位置 0~1）字段：生成时按列区间随机分配，
  同层房间区间互不重叠（保持从左到右顺序）；回归测试新增按 x 校验
  行内顺序与连线不交叉
- 地图按《杀戮尖塔 2》结构重构（`src/core/map.ts`）：每幕楼层数固定——
  第 1 幕 17 层、第 2 幕 16 层、第 3 幕 15 层（含首尾先古与 Boss 层），
  每个房间层固定 3 个房间；固定层机制：先古后的首层必为普通战斗、
  每幕中部固定宝箱层（第 1 幕第 10 层 / 第 2 幕第 26 层 / 第 3 幕第 41 层，
  整层宝箱）、Boss 前一层整层篝火；前 5 个房间层不出现精英/篝火
  （精英同时避开第 6 层，对齐 v0.101 补丁），每幕精英 4 个、商店 3 个
- 地图渲染重构（`src/ui/mapView.ts` + 样式）：每层固定成一行并标注层号
  （先古 / 首领 / 宝箱层 / 篝火层高亮），行带交替背景分组；
  层间连线改为竖直 S 曲线，配合生成器的单调约束（源左→右、目标列不递减）
  **线路不可能交叉**；地图支持长层数滚动与紧凑节点布局
- 地图结构回归测试：30 次 × 3 幕生成校验固定层数、固定层类型、
  同层不相连、连线不交叉、全员可达、无死路、精英/商店数量

- 新增 `dialogue.md` 项目对话纪要：按日期记录重要讨论结论与决策
  （8-14 全息工作台、8-15 效果文本自动生成与卡牌趣闻），附文档索引，
  作为 `docs/` 规范的决策时间线（8-13 废弃的旧日志形态不再使用）
- 战斗牌堆快捷键补全：`X` 打开/关闭消耗堆、`Z` 打开/关闭移除堆
  （与抽牌堆 A、弃牌堆 S、卡组 D 一致，按一次打开、再按一次关闭）
- 卡牌预览组件 `src/ui/cardPreview.ts`：悬停/选中卡按空格打开预览弹窗，
  弹窗中按 **Q** 或点击按钮在基础卡与升级卡（`<id>+`）之间互切，
  关键词与趣闻同步刷新；无升级版的卡不显示按钮
- `docs/todo-unimplemented-cards.md`：52 张未实现能力卡（50 张能力牌 + 2 张技能牌，
  打出无效果）清单与实现建议；293 张英文/混杂描述的本地化待办
- 被动钩子系统：`Effect.op: "passive"`（13 种触发时机：回合开始/结束、
  打出卡/攻击/技能、获得格挡、消耗卡、造成伤害、受到伤害、战斗结束、
  洗牌、抽牌、施加状态），能力牌打出时注册，与遗物共用 `hookDepth` 防递归
- 新状态：装甲（plating，受伤优先扣除）、活力（vigor，下次攻击加伤后归零）、
  保留（retain，回合结束保留手牌）、壁垒（barricade，格挡不清除）；
  宝珠槽上限可调（orbSlots，电容器等）
- 新效果：从弃牌堆取回并升级、加入随机卡（按类型/稀有度/去向）、
  变形手牌、抽抽牌堆顶牌、宝珠槽上限
- 修复 52 张未实现卡（全卡体检问题 A）：`src/data/passive_card_fixes.ts`
  补全 effects 与中文描述（数据驱动），全部可打出并产生可观察效果；
  多人与 Sovereign Blade 等未实现体系以近似效果替代并注明
- 卡牌本地化（全卡体检问题 B）：1040 张卡（548 基础 + 492 升级）描述
  英文残留归零——效果解析器新增被动结构化（每回合/每当类能力牌 effects
  结构化）、翻译器补 80+ 条规则与触发短语映射、46 张特殊机制卡手写中文
  描述表、数据构建层用 `describeEffects` 为结构化效果卡生成中文描述
  （含升级卡，fallback 卡以基础中文描述兜底）
- 新增「全卡中文」回归测试：构建后任何含英文残留的卡描述都会让测试失败

### Changed

- 战斗怪物卡**卡片化**（与卡牌同语言）：平面卡面 + 材质系统（普通/精英用合金、
  Boss 用闪卡 foil：彩虹旋转边框 + 光泽，复用卡牌材质变量）；布局重排——
  **攻击意图放卡牌上方**（图标 + 文字）、**血条 / 格挡 / 状态条放卡牌下方**
- 攻击与受击动画改为卡牌移动模拟：单目标卡打出时手牌飞向**目标怪物卡**、
  全体攻击飞向敌人区、对自身牌飞向玩家面板；怪物受击抖动（生命下降时）、
  新增玩家受击抖动（生命下降时玩家面板晃动）
- 地图图例改为**右侧完全固定浮动面板**：图例放在滚动容器（地图主体）之外
  绝对定位，任何窗口高度下滚动地图图例都纹丝不动（此前 sticky 在滚动
  起始段/接近底部时会跟随移动）；主地图与 F 浮层同步生效，
  悬停图例项 + 滚轮即可快速扫视同类房间分布
- `README.md` 重构为 GitHub 展示页：游戏介绍 / 核心亮点 / 快速开始 /
  文档导航，技术细节迁移至 `docs/DEVELOPMENT.md`；截图位预留待补充
- 地图节点状态可视化：**未到达不再半透明**，改为颜色/边框区分——
  未到达（正常蓝灰）、已到达（暗色背景 + 图标去饱和）、当前（金色）、
  可到达（蓝色高亮）；路线改为细暗背景线，**藏在节点图形后面**
  （节点不透明遮住穿行线，不再与地图重叠）
- `art-style.md §5` 背景规范更新为舞台剧式三景别：景深模糊（远 2px / 中 0px /
  近 1.5px，焦点在中景）、中近景视差飘动、远景静止，参数化接入演示页
- 地图房间不再三列严格对齐：同层房间按各自水平位置随机错位，
  连线按真实位置渲染并给每条边独立弧度，**线路不交叉/不重叠优先于列对齐**
- 地图头部与浮层文案：原「第 X 层」改为「第 X 幕 · 共 N 层」（此前把幕
  误标为层），浮层标题同步为「第 X 幕 · 地图」
- 战斗胜利卡牌奖励尺寸放大至约 3 倍面积（`--k: 0.92`，237×332），
  奖励面板加宽适配三张大卡
- CHANGELOG 按 Keep a Changelog 规范重组：合并同日重复分组、修复分类错误，
  并新增 `changelog-writer` Codex skill（含校验脚本 `check_changelog.py`，
  自动检查日期唯一/降序、分组重复与顺序、空条目）
- 效果文本自动生成：新增 `src/data/describe.ts` 生成器，从结构化 `effects`
  生成中文卡面文本（覆盖全部 Effect 类型，措辞对齐现有手写卡面，如
  「造成 5 点伤害 2 次」「将中毒层数翻倍」）；编辑器卡牌与升级描述新增
  「✨ 从效果自动生成」按钮；`buildDatabase` 构建期自动填充空描述
- 卡牌趣闻展示方式调整：卡面不再渲染 `flavor`（平时不显示），
  预览弹窗右侧新增「趣闻」区块
- 卡面展示统一：全部卡牌按**普通卡方案 A（画窗布局）**渲染，
  异画（满版布局）不再启用（数据 `artStyle` 字段保留，未来可恢复）；
  材质/光影/3D 倾斜等效果不变


- 全息膜按 `simeydotme/pokemon-cards-css` 源码重写（cosmos/regular holo 技法）：
  去掉绕卡中心旋转与平移动画，改为**固定斜条纹箔纸 + 鼠标驱动的
  background-position 视差**——主膜层 82° 斜条纹（repeating-linear-gradient、
  `background-size: 400% 900%`）随鼠标映射值（37~63 / 33~67）以 2.6 倍率滑动，
  副膜层（::before）以约 0.6 倍率错层制造深度；眩光层改为 radial-gradient +
  overlay 混合、亮度随「离卡中心距离」变化；新增「条纹角度 / 条纹宽度 / 对比度」
  滑杆，移除「漂移速度 / 漂移幅度 / 膜缩放」
- 普通卡全息改为区域化膜：画面区满强度（--art-film=1）、边框轻镀膜
  （--frame-film≈0.22）、扫光与星星保持整卡；新增「画面全息强度 / 边框全息强度」
  两个滑杆，异画卡仍整卡铺满
- 全息膜从硬边界色条改为**平滑插值彩虹**（regular-holo 风格）：颜色自然过渡，
  消除强烈线条感；「条纹宽度」滑杆改为「彩虹周期」（越大越平滑）
- 全息风格预设扩充到 **12 套**（对照 pokemon-cards-css 实卡效果）：
  宝可梦实卡 7 套（稀有 Rare / 宇宙 Cosmos / 反向 Reverse / 光辉 Radiant /
  奇观 Amazing / 金色 Secret / 闪光 Shiny）+ 自定 5 套（落日 / 极光 / 霓虹 /
  油膜 / 银河）；预设系统新增网格纹理（--holo-mesh）、副膜混合模式可换
  （--holo-sub-mix）、区域强度覆盖（--art-film / --frame-film）

### Fixed
- 修复怪物卡内容被卡面底版遮挡（战斗中只看到 emoji）：`enemy-face` 为绝对定位
  `z-index:0` 的不透明底版，static 内容绘制在其下方被盖住——内容层（意图/名字/
  血条/状态）统一提升 `z-index:4`，浮在材质层（2/3）之上；血条红条经像素采样确认可见
- 牌堆悬停提示与实际快捷键不符：四个牌堆（抽/弃/消/移）提示全部写死
  "快捷键 A/S/D"，但消耗堆/移除堆原本没有快捷键——现按各自真实按键提示
  （A / S / X / Z），卡组按钮补 "D" 提示
- 全卡体检发现 17 张卡描述为空导致卡面效果区空白：构建期自动填充后全部有文本
  （仅剩 3 张 effects 也为空的完全空卡，见 `docs/todo-unimplemented-cards.md`）
- 技能/能力卡画窗形状从未生效：`shape-pentagon / shape-ellipse` 的
  `clip-path` 因形状 class 放在画窗元素自身（选择器要求父级）而永不匹配，
  画窗一直显示为矩形。已按 studio 结构把形状 class 移到卡牌父级，
  并恢复 studio 亮银边框，用无头浏览器逐值对比验证：
  技能（五边形）/能力（椭圆）画窗与 studio 一致


- 膜运动模型错误修正：此前的 conic 绕心旋转、以及后来过渡版的「translate 平移动画 +
  transform 视差」都不是 repo 的真实做法；`--holo-angle` 语义改为条纹角度
- 修复普通卡开全息后卡名 / 费用 / 描述区被彩虹条纹盖住的「渲染不对」：
  膜改按画面区 / 边框区分层后，文字区恢复深色可读，画面区正常闪箔
- 修复 range 滑杆 step 吸附：`step="0.05"` 会把 `value="0.22"` 吸附成 `0.2`，
  全息强度滑杆改用 `step="0.01"` 保证精确值
- 修复全息膜硬边界色条的线条感：实心色带（c1 0-4%, c2 4-8%）边缘锐利，
  改为平滑插值渐变后颜色连续过渡；剩余硬跳变仅为像素画自身的高光块

### Removed
- 美工目录清理：删除早期综合预览草稿 `docs/art-mockup.html`（卡牌材质/怪物动画/
  背景分层已分别由 art-card-studio / art-monster-material / art-background-material
  三个专门演示页覆盖）、临时 QA 脚本 `scripts/_qa_card_demo.cjs`；
  `docs/assets/sparkles.gif` 与 `assets/ui/sparkles.gif` 完全重复，统一保留后者
  并更新全部引用（docs 只留规范与讨论）

## 2026-08-14

### Added
- 卡牌美术工作台升级为全息参数工作台（`docs/art-card-studio.html`）：
  - 6 套全息风格预设（彩虹 / 落日 / 极光 / 霓虹 / 油膜 / 银河），
    与未来 `CardData.foil` 字段一一对应，点按即整套切换 conic 色环 /
    饱和度 / 亮度 / 膜转速 / 星星大小与密度
  - 全息可调参数：膜厚度 / 旋转速度 / 起始角度 / 缩放 / 饱和 / 亮度 / 模糊 /
    混合模式（自动跟随方案，可手动覆盖）、光斑强度与大小、扫光强度 / 角度 /
    模糊 / 带宽 / 频率 / 速度、星星强度 / 大小 / 密度 / 闪烁速度、
    塑料光泽强度、3D 倾斜幅度与自动旋转速度
  - 控制区改为分组折叠（总开关 / 风格预设 / 全息膜 / 光斑·扫光 / 星星 / 光泽·3D），
    每个滑杆实时显示当前值，支持一键重置全息参数


- 移除池机制：`PlayerCombatState` 新增 `removedPile`，能力牌打出后进入独立的
  「移除池」（本场战斗暂时移除，下一场照常可用），与消耗堆完全分离——
  未来「从消耗堆拉回牌」类机制不会误伤能力牌；战斗界面新增「移除」牌堆按钮可查看
- 磨砂玻璃卡面：全部卡牌统一改为磨砂玻璃材质（`backdrop-filter` 模糊 +
  半透明白渐变 + 高光带 + 类型色边框/内辉光），颜色沿用卡牌当前的
  `--card-accent`；与 `docs/art-card-material.html` 的 `.mat-glass` 语言一致
- 版本/拓展包字段：`CardData` / `RelicData` 新增 `version`（字符串，如
  「基础版」「DLC1」），编辑器卡牌/遗物表单可配置，为未来拓展包（DLC）铺路
- 先古事件：每层第一个节点固定为先古事件（地图入口节点），进入后先回复缺失
  生命值的 X%（默认 100 = 满血，全局设置可覆盖），再随机获得一件先古遗物
- 先古角色数据：新增 `AncientData` 与 `ancients` 数据段（`data/ancients.json`），
  内置「先古旅人」角色与 4 件先古遗物（先古之镜/核心/图腾/罗盘，仅进先古池）；
  DIY 编辑器新增「先古」页签，可配置角色、回血比例与遗物池
- 全局设置新增「先古回血比例 %」与「先古角色」两项


- 角色系统：新增 5 个 STS2 初始角色（铁甲战士 / 静默猎手 / 储君 / 亡灵契约师 / 故障机器人），
  各自带起始生命、起始牌组与初始遗物（燃烧之血 / 蛇之戒 / 神圣权柄 / 绑定魂匣 / 破损核心）；
  主菜单支持选角色开新局，战斗中面板显示角色名
- STS2 基础卡包（代号 `STS2`）：为 5 个角色新增约 35 张专属卡牌
  （恶魔形态、收割、中和、催化剂、刀舞、爪击、缓冲、收割灵魂、死亡行军、七星、虚空形态等），
  现有全部卡牌/遗物/怪物统一归入 STS2 包
- 数据结构扩展：`CardData` / `RelicData` 支持 `character`（留空 = 无色/通用）与
  `pack`（拓展包代号）；新增 `CharacterData` 与 `characters` 数据段（data/characters.json）；
  卡牌奖励与商店按当前角色过滤（只出角色专属 + 无色卡）
- 遗物触发新增两个时机：`cardExhausted`（消耗/移除卡牌时）、`receiveDamage`（受到伤害时）；
  新增余烬核心、铁魂、反应装甲等遗物，扩展 DIY 空间
- STS2 Boss：新增墨影幽灵、仪式兽、知识恶魔、帝皇蟹、女王、实验体 6 个首领，
  各层 Boss 从对应池随机（第 1/2/3 层各有专属 Boss 池）
- 怪物两帧待机动画：9 种怪物各自独立的双姿态 CSS 动画
  （squish / breathe / rock / bob / sway / hover / float），`EnemyData.anim` 字段可配置，
  未来像素帧图就绪后 anim 可直接映射 sprite sheet 待机行
- 卡牌悬停 3D 倾斜：鼠标位置驱动 `rotateX/rotateY`，镜面高光（`--lx/--ly`）跟随鼠标，
  与 `docs/art-card-material.html` 演示一致


- 星辰资源（储君）：战斗内资源系统，卡牌支持 `starsCost` 门槛；坠星/尊崇产星、
  七星/调和/星陨耗星、星辉（starlight）状态每回合产星；神圣权柄每场战斗开始给 3 星
- 灵魂资源（亡灵契约师）：卡牌支持 `soulsCost`；护卫/释放/收割灵魂/吞噬生命产魂、
  灵魂风暴耗魂、灵魂涌动（soulflow）状态每回合产魂
- 宝珠系统（故障机器人）：3 槽宝珠（闪电/冰霜/黑暗/玻璃），回合开始被动触发
  （闪电随机伤害、冰霜格挡、黑暗蓄力、玻璃衰减全体伤害），打出宝珠爆发伤害/格挡；
  破损核心每场引导 1 颗闪电；新增集中（focus）、裂变卡牌
- 召唤系统（亡灵契约师）：骷髅护卫能替玩家挡刀、每回合开始攻击随机敌人，
  可被释放/召唤卡重新召唤；绑定魂匣改为战斗开始时召唤
- 灾厄（Doom）：负面状态累计 10 层时敌人被处决，回合结束 -1；
  萦绕/死亡印记/死亡行军可施加灾厄
- 药水系统：新增 `PotionData` 与 `potions` 数据段（data/potions.json）+ 编辑器「药水」页签；
  战斗内使用（背包上限 3 瓶）、战斗胜利概率掉落、商店出售、事件选项 `addPotions`；
  内置 8 种初始药水（烈焰/鲜血/力量/敏捷/能量/抽牌/剧毒/铁壁）
- 消耗堆互动：新增 `retrieveFromExhaust` 效果（掘墓卡可从消耗堆取牌）；
  能力牌在独立移除池，永远不会被消耗堆互动误拉回
- `apply` 效果支持 `allEnemies` 目标（修复全体施加类药水/卡牌无法生效的问题）
- STS2 全卡池：新增 488 张卡（铁甲战士 / 静默猎手 / 故障机器人 / 亡灵契约师 /
  储君 / 无色），覆盖 EA 0.98.0 全部角色与无色卡；卡名/描述/费用/稀有度/升级版
  为游戏真实数据，可解析的效果（伤害/格挡/抽牌/能量/状态/宝珠/星辰/灵魂/灾厄/
  召唤/消耗互动）已精确映射，复杂机制卡（约 148 张，如锻造、X 费用、条件增伤）
  用默认近似并在描述中保留原文，可在编辑器里精修
- 卡牌生成工具：`scripts/generate_sts2_cards.py` 从 Flex Games Wiki 的
  `sts2-data/cards.json` 自动生成 `src/data/sts2_cards.ts`，官方数据更新后可重跑；
  生成时自动跳过与手工精修卡冲突的 id（33 张），避免覆盖现有设计
- 卡牌中文化：521 条卡名翻译表写入生成器（卡名 100% 中文）；
  效果描述规则化翻译（伤害/格挡/抽牌/能量/状态/宝珠/星辰/灵魂/灾厄等高频句式），
  特殊句保留英文原文并列入 `docs/content-roadmap.md` 逐张精修
- 状态图标化：全部状态以图标 + 层数显示（`STATUS_ICONS`），悬停浮窗解释；
  卡牌「消耗/虚无/打出后移除」等标签、遗物、药水、星辰/灵魂资源、宝珠、
  召唤物均支持悬浮说明（新增 `src/ui/tooltip.ts`）
- 卡面消耗图形化：星辰/灵魂费用显示为卡面徽标（替代纯文字 tag）
- 游戏内弹窗：新增 `src/ui/modal.ts`，替换全部 22 处浏览器 alert/confirm
  （退出战斗/移除卡牌/升级卡牌/清空临时调整/绑定文件夹等）
- 新增 `docs/content-roadmap.md`：内容丰富化路线图（翻译精修、引擎机制补全、
  趣味卡面文案、关键词库等），每轮更新进度
- 新机制·计数器/延迟效果：`addCountdown` 效果（「下回合 / X 回合后」结算），
  战斗面板显示倒计时徽标（炸弹、延迟抽牌等 20 张卡接入）
- 新机制·锻造：`forge` 效果升级手牌中随机实例（储君 Forge 体系 13 张卡接入）
- 新机制·弃牌与奇巧：`discard` 效果（弃置手牌）+ `CardData.sly` 弃牌触发
  （为 Sly 关键词铺路；回合结束弃手牌同样触发）
- 新机制·条件增伤：`DamageScaling` 按消耗堆/格挡/易伤/本回合打出/手牌/中毒
  等线性加成（灰烬打击、欺凌、终结技等 26 张卡接入）
- 卡面丰富化：`CardData.flavor` 风味文案（46 张招牌卡）、稀有度材质分级
  （starter/common/uncommon/rare，金卡/闪卡 class 预留）、状态徽章样式统一
- 浮窗接入补全：商店（卡牌/遗物/药水）、事件选项、精英遗物奖励、遗物总览
- 编辑器：卡牌表单新增「风味文案」字段
- 手牌弧线修正：中间牌平放、两侧牌外旋下垂（左逆时针/右顺时针），
  模拟人类握牌习惯（`--arc-y` / `--arc-rot` 由战斗界面按位置设置）
- 手牌悬停交互：悬停放大 1.5 倍 + 水平化（旋转归零）+ 上浮，
  特异性提权修复此前缩放被通用悬停规则覆盖的问题
- 卡牌存储格式确认与增强：新增 `CardData.material`（卡面材质，
  cloth/silver/alloy/gold/foil，缺省按稀有度派生，为金卡/闪卡铺路）；
  `art` 支持图片路径/URL（自动渲染 `<img>`，未来卡图直接可用）；
  编辑器卡牌表单新增「卡面材质」下拉
- 卡牌存储格式补充（对齐美工 v4 卡面方案）：新增 `artStyle`（普通卡=画窗布局 /
  异画卡=满版布局）、`scheme`（v4 方案代号 A-G）、`number`（卡图编号）、
  `artScale/artX/artY`（卡图定位参数）
- 卡牌图鉴统一展示顺序：编辑器卡牌列表**普通卡（画窗布局）在前、
  异画卡（满版布局）在后**，同组按 id；异画卡在列表带「异画」标记
- 游戏内卡面切换为美工 v4 卡面（docs/art-card-layout_v4.html）：
  普通卡 = 画窗布局（费用球/类型宝石/银框画窗/描述板/底部编号·类型·稀有度
  信息条/稀有度宝石/升级 + 标记），异画卡 = 满版布局（图片铺满整卡、
  悬浮 UI、毛玻璃描述板）；类型画窗形状沿用（攻击矩形/技能五边形/能力椭圆，
  异画不受裁剪）
- 卡面尺寸统一由 `--k` 缩放系数驱动：手牌 / 小卡 / 大卡 / 预览大卡共用同一套
  v4 布局；`artScale/artX/artY` 定位参数已接入画窗图片；稀有度/颜色暂为简单填充
- 保留全部游戏特性：悬停 3D 倾斜、手牌弧线、选中高亮、星辰/灵魂资源徽标、
  消耗/虚无/奇巧标签、风味文案、金卡/闪卡材质 class
- 卡面材质/光影/倾斜接入美工定稿（`docs/art-card-studio.html`）：
  六种材质（粗布/银灰/合金/金箔/闪卡/玻璃，渐变边框 + 卡体背景 + 高光强度）、
  镜面高光跟随鼠标（`--lx/--ly`）、闪卡彩虹旋转边框 + 扫光、
  3D 倾斜幅度加大（±9°/±12°）；预览大卡浮空动画移到外层容器，悬停倾斜生效
- 手牌放大：战斗手牌尺寸提升（155×217，`--k: 0.62`），选中时再放大 1.07 倍，
  悬停 1.5 倍保持；手牌区高度同步加大
- 牌堆视图重做：手牌两侧显示卡背堆叠——左下=抽牌堆（上方=移除堆）、
  右下=弃牌堆（上方=消耗堆），带数量角标与悬浮说明，点击打开牌堆列表
  （A/S/D 快捷键保持）
- 牌堆动画：抽牌时新入手的牌从抽牌堆位置飞入手牌；抽牌堆从空变为有牌时
  播放洗牌抖动；四个牌堆数量变化时数字角标跳动
- 牌堆间转移动画（通用机制）：战斗引擎记录 `pileMoves` 转移事件流，
  洗牌（弃牌堆→抽牌堆）、弃牌效果（手牌→弃牌堆）、消耗堆取回（→手牌）
  均播放牌背飞行精灵 + 目标牌堆跳动；后续新增牌堆互动只需在引擎加事件记录

### Changed
- 「效果预览」区重组为全息参数工作台：先选风格预设，再按组微调


- 能力牌落点调整：由「进消耗堆」改为「进独立移除池」，卡面标签改为「打出后移除」；
  消耗堆从此只放显式标记 `exhaust` 的卡牌

### Fixed
- `--holo-angle` 此前被动画覆盖 transform 导致无效：改为写入
  `@keyframes holo-spin` 起点（from rotate(var(--holo-angle))），实测生效
- 异画卡（方案 E/F/G）亮底混合模式：原实现仅膜层硬编码 overlay 且选择器写错
  （alt-mode 在 body、foil-on 在卡面元素），现统一由 `--holo-mix` 驱动，
  `body.alt-mode .foil-on .holo-*` 自动切 overlay，控制台可手动覆盖


- 荆棘反伤方向错误：修复为「怪物攻击带荆棘的玩家时怪物自损、玩家攻击带荆棘的敌人时玩家受伤」
  （此前误用敌方荆棘值且方向相反，导致双方荆棘都不生效）
- 切换手牌选中/取消（点击、数字键、ESC）不再全量重建战斗界面，
  改为轻量更新手牌选中态与敌人高亮——怪物区/玩家区保持独立稳定，
  怪物位置与动画状态不再被重置
- 技能/能力牌画窗在五边形/椭圆裁剪下银框边缘过亮（"白圈"），
  当时形状画窗改用低调金属色描边（后续 08-15 已恢复 studio 亮银框）

## 2026-08-13

### Added
- 能力牌机制：`type: "power"` 的卡牌打出后不进入弃牌堆（08-14 起进独立「移除池」，见上）
- 事件选项 `addRelicPool`：从指定池随机获得一件未拥有的遗物
- 池子系统：`CardData` / `RelicData` 新增 `pools`（`reward` / `shop` / `boss` / `event`），奖励、商店、Boss 按池过滤，池空回退全量
- 精英遗物奖励：击败精英后随机获得一件遗物（reward 池），再选卡牌
- Boss 卡牌奖励：击败首领后先选一张 Boss 稀有度加权的卡牌，再进入下一层
- 项目文档：新增 `AGENTS.md`（项目级 Codex 指令）、`GETTING_STARTED.md`（新设备初始化教程）、`.gitignore`；开发日志改用 CHANGELOG 规范

### Changed
- 环境：项目迁移至 Mac 并接入 Git 托管，Node v25.9.0 / npm 11.12.1，`.codex/config.toml` 的 writable_roots 更新为当前路径
- 文档：`README.md` 环境说明改为引用初始化教程；原有跨设备交接文档拆分职责（玩家向 → `GETTING_STARTED.md`，开发者向 → `AGENTS.md`）

### Fixed
- `README.md` 已完成功能列表编号重复（两个 8），已重新编号为 1-12

### Removed
- 废弃 `dialogue.md`（命名不规范，开发日志迁移至本文件）
- 删除交接文档 `DEV_HANDOVER.md`（内容按玩家/开发者职责拆分到 `GETTING_STARTED.md` 与 `AGENTS.md`）
