# dialogue.md —— 项目对话纪要

本文件记录项目中的重要讨论结论与决策，详细规范以 `docs/` 下对应文档为准
（单一权威原则：同一件事只在一个文档里维护，本文件只做索引与时间线）。

## 2026-08-14 · 全息参数工作台（卡牌美术）

### 结论

- 卡牌美术统一入口：`docs/art-card-studio.html`（材质 / 动画 / 布局 一体，
  本次升级为「全息参数工作台」）。
- 全息采用宝可梦式多层伪立体：彩虹膜 + 跟随鼠标光斑 + 扫光 + 星星 + 塑料光泽，
  技法沉淀在 `docs/emboss.md`。
- 内置 **12 套全息风格预设**，与未来 `CardData.foil` 字段一一对应：
  宝可梦实卡 7 套（稀有 Rare / 宇宙 Cosmos / 反向 Reverse / 光辉 Radiant /
  奇观 Amazing / 金色 Secret / 闪光 Shiny，均对照 pokemon-cards-css 实卡效果）
  + 自定 5 套（落日 / 极光 / 霓虹 / 油膜 / 银河）；
  预设之上可逐项微调（膜 / 光斑 / 扫光 / 星星 / 光泽 / 3D）。
- 混合模式默认「自动」：普通卡 color-dodge、异画卡（方案 E/F/G）overlay，
  可手动覆盖。
- 材质自带的静态镜面高光与全息扫光是两个参数（`--mat-gloss` vs `--gloss-a`），
  已解耦，选材质后扫光滑杆依然有效。

### 踩坑（已修）

- 膜不做绕卡中心旋转（风车观感错误）。对照 pokemon-cards-css 源码后确认正确做法：
  固定斜彩虹（repeating-linear-gradient，颜色平滑插值无硬边）+
  `background-position` 随鼠标视差滑动，
  眩光层（radial-gradient + overlay）随鼠标移动、亮度随离中心距离变化；
  膜图案本身不旋转不漂移。
- 全息膜不能用硬边界色条（c1 0-4%, c2 4-8%）：实心色带边缘锐利 = 强烈线条感；
  必须用平滑插值渐变（纯色列表，regular-holo 风格）。
- 普通卡膜必须区域化：宝可梦卡是全幅画，膜铺满没问题；本项目普通卡是
  「小画窗 + 深色 UI 边框」，膜铺满会变成彩虹条码盖住文字。
  做法：画面区满强度（--art-film=1）、边框轻镀膜（--frame-film≈0.22）、
  扫光/星星保持整卡；异画卡仍整卡铺满。
- 异画卡自动切 overlay 的选择器：
  `body:not(.mix-manual).alt-mode .foil-on .holo-film`
  （alt-mode 在 body、foil-on 在卡面元素，不在同一元素上）。
- range 滑杆 step 会吸附默认值（如 step=0.05 时 0.22 变 0.2），
  需要精确小数时 step 用 0.01。

### 文档索引

- 卡牌表面工艺（浮雕 / 全息）：`docs/emboss.md`
- 美术风格规范（像素角色 + 3D 材质界面）：`docs/art-style.md`
- 材质 / 动画 / 布局演示：`docs/art-card-studio.html`、
  `docs/art-monster-material.html`、`docs/art-background-material.html`

## 2026-08-15 · 效果文本自动生成 + 卡牌趣闻（游戏侧）

### 结论

- 效果文本采用「保留 description 字段 + 生成器辅助」：`src/data/describe.ts`
  从结构化 `effects` 生成中文文本，编辑器提供「从效果自动生成」按钮，
  `buildDatabase` 构建期自动填充空描述。
- 趣闻 `flavor` 平时不显示在卡面；预览弹窗（`showCardPreview`）右侧展示。
- 全卡体检（548 基础卡）：52 张卡 effects 为空且战斗引擎未实现（能力牌白打）、
  293 张描述为英文/混杂、17 张描述为空（已自动填充）。
- 未实现卡与英文本地化单开一轮，开工单见
  `docs/todo-unimplemented-cards.md`。

## 2026-08-15 · 地图重构（固定层数 + 固定宝箱/篝火层 + 不交叉连线）

### 尖塔 2 地图机制调研结论

- 每幕**层数固定**：第 1 幕（密林/暗港）17 层 = 先古 + 15 房间层 + Boss，
  第 2 幕（巢穴）16 层 = 先古 + 14 + Boss，第 3 幕（荣耀）15 层 =
  先古 + 13 + Boss；每层房间数量不固定（1-7 个），但**同层房间绝不互相连接**。
- 标志性楼层固定：第 1/18/34 层是先古之民房间；其下一层必为普通战斗；
  每幕中部（第 10/26/41 层）必为数个宝箱房；Boss（第 17/33/48 层）
  的前一层必为数个休息处。
- 房间分布（每幕）：精英 5 个（进阶 1+ 为 8）、商店 3 个、未知 9-14、
  篝火 5-7、战斗占剩余；**前 5 层不出现精英与篝火**，v0.101 起第 6 层
  也不再刷精英。

### 本项目落地

- 保留 3 列设计（每房间层固定 3 个房间），按幕固定层数 17/16/15。
- 固定层：首层（先古后）全员战斗、中部宝箱层整层宝箱、Boss 前一层整层篝火。
- 精英每幕 4 个（每行 1 个、可绕行，避开前 6 层与固定层）；商店每幕 3 个；
  额外篝火小概率出现在第 6 个房间层之后；其余行按战斗/事件 55/45 生成。
- 连线重写为**单调不交叉**算法：源从左到右、目标列不递减、只连相邻列；
  渲染用层内竖直 S 曲线（cubic Bezier），物理上不可能交叉。
- 展示：每层一行 + 层号标签（先古/首领/宝箱层/篝火层高亮），行带交替背景，
  地图可滚动；地图头部显示「第 X 幕 · 共 N 层」。
- 追加：**列不严格对齐**——同层房间在行内按 `MapNode.x`（0~1）随机错位，
  但保持从左到右的顺序（区间互不重叠），连线按真实位置绘制并给每条边
  独立弧度（稳定哈希），保证**线路不交叉/不重叠优先于列对齐**。
- 追加：**节点状态可视化**——`RunState.visitedNodes` 记录本幕已进入节点，
  地图上未到达/已到达/当前/可到达用颜色与边框区分（未到达不透明、蓝灰；
  已到达暗色 + 图标去饱和；当前金色；可到达蓝色高亮）；路线改细暗背景线，
  节点不透明遮住穿行线，线不再和节点重叠。
- 追加：**地图图例**——除战斗外的类型（宝箱/事件/篝火/精英/商店/先古/首领）
  图标说明，悬停图例项时该类型节点金色高亮、其余节点临时变暗，移出恢复；
  主地图与 F 浮层均可用；图例改为**右侧完全固定浮动面板**（放在滚动容器
  外绝对定位，sticky 在滚动起始段/底部会移动，故弃用），任何窗口高度下
  滚动地图图例都纹丝不动，悬停图例 + 滚轮可快速扫视同类房间分布。
- 回归测试：30 次 × 3 幕生成，校验固定层数/类型、同层不相连、不交叉、
  全员可达、无死路、精英/商店数量；按 x 校验行内顺序与连线不交叉。

### 待定 / 后续

- 精英数量按 4 实现（原版 5）：本项目精英必掉随机遗物，数量偏保守，
  后续可在 `src/core/map.ts` 的 `pickDistinctRows(4, ...)` 调整。
- 后续可做：每层房间数量差异化（1-7 列）、Boss 前置精英层、藏宝图遗物
  （路线汇聚至特定宝箱）、进阶（Ascension）对房间分布的影响。

## 2026-08-15 · 背景美术：舞台剧式三景别

### 结论

- 卡牌美术暂缓，先做背景：舞台剧式远中近三景别
  （远景=天幕布景 / 中景=表演区 / 近景=台口剪影）。
- 景深用三景别独立高斯模糊模拟，焦点默认在中景
  （远 2px / 中 0px / 近 1.5px，可调 + 预设：舞台浅景深 / 微景深 / 全清晰）。
- 默认主题「星火荒原」（夜晚）：远景星空/银河/月亮、中景火山群、
  近景草地（草叶风吹摆动）；抽象渐变主题保留为备选。
- 中景与近景做缓慢视差飘动（`drift-x`，幅度/周期可调、可开关），远景静止；
  `prefers-reduced-motion` 与手动开关均停用。
- 演示页：`docs/art-background-material.html`（含本地图片中景替换）；
  规范更新：`docs/art-style.md §5`。
- 怪物美术与任务角色美术暂缓为 TODO：
  `docs/todo-monster-character-art.md`（像素 Sprite Sheet、引擎接入、
  角色立绘字段，验收标准见文档）。

## 2026-08-15 · 战斗呈现：怪物卡片化（角色美术方向）

### 结论

- 战斗中的怪物采用**平面卡片呈现**（与卡牌同材质语言）：普通/精英=合金、
  Boss=闪卡 foil（彩虹边框 + 光泽），不再走像素站场路线。
- 攻击 / 受击用卡牌移动模拟：手牌飞向目标怪物卡（单目标）、飞向敌人区
  （全体）、飞向玩家面板（对自身）；怪物与玩家受击时抖动。
- 怪物卡布局：**攻击意图在上方**，**血条 / 格挡 / 状态条在下方**。
- 像素 Sprite 方案暂缓；`docs/todo-monster-character-art.md` 保留，
  角色/怪物美术后续按卡片化语言细化。
